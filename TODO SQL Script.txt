--1.) Create Database and Connect

--2.) Create table ACCOUNT should have : firstname , lastname , email , password , tell
CREATE TABLE ACCOUNT(
EMAIL VARCHAR(30) NOT NULL PRIMARY KEY,
PASSWORD VARCHAR(20) NOT NULL,
FIRSTNAME VARCHAR(30) NOT NULL,
LASTNAME VARCHAR(30) NOT NULL,
TELL VARCHAR(10) NOT NULL,
ADDRESS VARCHAR(350) NOT NULL,
DEBITCARD VARCHAR(16) NOT NULL);

--3.) Create table PRODUCT
CREATE TABLE PRODUCT(
PRODUCTCODE VARCHAR(10) NOT NULL PRIMARY KEY,
PRODUCTBRANDNAME VARCHAR(40) NOT NULL,
PRODUCTLINE VARCHAR(50) NOT NULL,
PRODUCTTYPE VARCHAR(50) NOT NULL,
PRODUCTSEX VARCHAR(5) NOT NULL,
PRODUCTSIZE INT NOT NULL,
PRODUCTPRICE DECIMAL NOT NULL,
PRODUCTDESCRIPTION VARCHAR(120),
QUANTITYINSTOCK INT;

--4.) Create table ProductLine
CREATE TABLE ProductLine(
PRODUCTLINE VARCHAR(10) NOT NULL PRIMARY KEY,
TEXTDESCRIPTION VARCHAR(120));

--5.) Create table HISTORY
CREATE TABLE HISTORY(
PRODUCTCODE VARCHAR(10) NOT NULL PRIMARY KEY,
EMAIL VARCHAR(30) NOT NULL,
PRODUCTBRANDNAME VARCHAR(40) NOT NULL,
PRODUCTLINE VARCHAR(50) NOT NULL,
PRODUCTTYPE VARCHAR(50) NOT NULL,
PRODUCTSIZE INT NOT NULL,
PRODUCTPRICE DECIMAL NOT NULL,
CONSTRAINT historyProduct FOREIGN KEY (ProductCode) REFERENCES Product (ProductCode) ON DELETE NO ACTION ON UPDATE NO ACTION,
CONSTRAINT accountEmail_history FOREIGN KEY (EMAIL) REFERENCES account (EMAIL) ON DELETE NO ACTION ON UPDATE NO ACTION);